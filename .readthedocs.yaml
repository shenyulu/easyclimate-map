version: "2"

python:
  install:
    - requirements: docs/requirements-docs.txt

sphinx:
  configuration: docs/source/conf.py

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.12"
  apt_packages:
    - optipng
